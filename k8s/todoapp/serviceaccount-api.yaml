apiVersion: v1
kind: ServiceAccount
metadata:
  name: todoapi
  namespace: todoapp
  annotations:
    # Bound to UAMI uami-todoapi via federated identity credential
    # (see terraform/cosmos.tf — azurerm_federated_identity_credential.todoapi).
    azure.workload.identity/client-id: "REPLACE_TODOAPI_UAMI_CLIENT_ID"
