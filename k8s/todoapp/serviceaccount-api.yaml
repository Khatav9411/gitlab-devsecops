apiVersion: v1
kind: ServiceAccount
metadata:
  name: todoapi
  namespace: todoapp
  annotations:
    # Bound to UAMI uami-todoapi via federated identity credential
    # (see terraform/cosmos.tf — azurerm_federated_identity_credential.todoapi).
    azure.workload.identity/client-id: "2ed90d51-b3e9-43d9-ada1-26f23bbde7bf"
